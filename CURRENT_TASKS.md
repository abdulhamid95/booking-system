# CURRENT_TASKS.md

> هذا الملف يحتوي فقط على المهام النشطة حالياً.  
> بقية المهام موجودة في [BACKLOG.md](BACKLOG.md) وستُضاف هنا عند حلول دورها.  
> المهام المنجزة تُنقل إلى [DONE.md](DONE.md) بموافقة مدير المشروع.

---

## EPIC-01: إعداد المشروع والبنية الأساسية

> **الحالة:** نشطة — يجب إنجاز هذه المهام قبل أي شيء آخر.

---

### TASK-001-A: تثبيت المتطلبات وإنشاء البيئة الافتراضية

**الأولوية:** P1  
**النوع:** Setup

**الوصف:**  
تجهيز بيئة التطوير المحلية قبل إنشاء المشروع.

**المهام الفرعية:**
- إنشاء virtual environment باستخدام `python -m venv venv`
- تفعيل البيئة الافتراضية
- تثبيت المكتبات الأساسية:
  - `Django`
  - `djangorestframework`
  - `python-decouple`
  - `psycopg2-binary`
- إنشاء ملف `requirements.txt` بـ `pip freeze > requirements.txt`

**معايير القبول:**
- البيئة الافتراضية تعمل
- جميع المكتبات مثبتة بدون أخطاء
- ملف `requirements.txt` موجود ومحدّث

- [ ] منجزة

---

### TASK-001-B: إنشاء هيكل مشروع Django

**الأولوية:** P1  
**النوع:** Setup

**الوصف:**  
إنشاء مشروع Django الأساسي والتحقق من أنه يعمل.

**المهام الفرعية:**
- تنفيذ `django-admin startproject core .`
- التحقق من أن `python manage.py runserver` يعمل بدون أخطاء

**معايير القبول:**
- المشروع ينطلق محليًا على المنفذ الافتراضي
- لا توجد أخطاء عند التشغيل الأول

- [ ] منجزة

---

### TASK-001-C: إنشاء التطبيقات الأساسية

**الأولوية:** P1  
**النوع:** Setup

**الوصف:**  
إنشاء تطبيقات Django التي تمثل وحدات النظام المختلفة.

**المهام الفرعية:**
- تنفيذ `python manage.py startapp` لكل تطبيق:
  - `accounts` — إدارة المستخدمين
  - `businesses` — إدارة المنشآت
  - `services` — إدارة الخدمات
  - `staff` — إدارة الموظفين
  - `bookings` — إدارة الحجوزات
- إضافة جميع التطبيقات إلى `INSTALLED_APPS` في `settings.py`

**معايير القبول:**
- جميع التطبيقات الخمسة موجودة كمجلدات
- جميعها مسجلة في `INSTALLED_APPS`
- لا يوجد خطأ عند تشغيل المشروع بعد الإضافة

- [ ] منجزة

---

### TASK-001-D: إعداد ملف البيئة (.env)

**الأولوية:** P1  
**النوع:** Setup

**الوصف:**  
فصل الإعدادات الحساسة عن الكود باستخدام ملف `.env`.

**المهام الفرعية:**
- إنشاء ملف `.env` في جذر المشروع يحتوي على:
  - `SECRET_KEY`
  - `DEBUG`
  - إعدادات قاعدة البيانات
- إنشاء ملف `.env.example` كنموذج للمطورين (بدون قيم حساسة)
- إضافة `.env` إلى `.gitignore`
- تعديل `settings.py` لقراءة القيم من `.env` عبر `python-decouple`

**معايير القبول:**
- لا توجد قيم حساسة مكتوبة مباشرةً في `settings.py`
- المشروع يعمل بعد قراءة الإعدادات من `.env`
- ملف `.env` غير مُتتبَّع في git

- [ ] منجزة

---

### TASK-001-E: إعداد Django REST Framework

**الأولوية:** P1  
**النوع:** Setup

**الوصف:**  
تهيئة DRF لأن المشروع سيعتمد API لخدمة الصفحات العامة والحجوزات.

**المهام الفرعية:**
- التأكد من تثبيت `djangorestframework`
- إضافة `rest_framework` إلى `INSTALLED_APPS`
- إضافة الإعدادات الأساسية لـ DRF في `settings.py`:
  ```python
  REST_FRAMEWORK = {
      'DEFAULT_PERMISSION_CLASSES': [
          'rest_framework.permissions.IsAuthenticatedOrReadOnly',
      ],
  }
  ```

**معايير القبول:**
- `rest_framework` موجود في `INSTALLED_APPS`
- المشروع يعمل بدون أخطاء بعد الإضافة

- [ ] منجزة

---

## ملخص المهام النشطة

| المهمة | العنوان | الحالة |
|--------|---------|--------|
| TASK-001-A | تثبيت المتطلبات وإنشاء البيئة الافتراضية | ⬜ |
| TASK-001-B | إنشاء هيكل مشروع Django | ⬜ |
| TASK-001-C | إنشاء التطبيقات الأساسية | ⬜ |
| TASK-001-D | إعداد ملف البيئة (.env) | ⬜ |
| TASK-001-E | إعداد Django REST Framework | ⬜ |
